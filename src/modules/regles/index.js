const Regle = require("./regle.model");
const regleController = require("./regle.controller");
const regleRouter = require("./regle.routes");

module.exports = {
  Regle,
  regleController,
  regleRouter
};
